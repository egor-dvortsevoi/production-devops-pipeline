image:
  repository: felixdvrt/production-devops-pipeline
  tag: 0293c16
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false

serviceAccount:
  enabled: false

httpRoute:
  enabled: false