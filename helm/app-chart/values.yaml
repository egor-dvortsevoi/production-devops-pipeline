image:
  repository: felixdvrt/production-devops-pipeline
  tag: dfca60d
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false

serviceAccount:
  enabled: false